# Cloudflare Tunnel Environment Configuration
# Use this for Cloudflare Tunnel deployments

# Security - REQUIRED: Change this to a random secret key
JWT_SECRET=your-random-jwt-secret-key-here-min-32-characters

# Cloudflare Tunnel Configuration
# Set this to your Cloudflare Tunnel URL or custom domain
CLIENT_URL=https://your-app.pages.dev

# Alternative for custom domain:
# CLIENT_URL=https://app.yourdomain.com

# Timezone - Set to your preferred timezone
TZ=UTC
TIMEZONE=UTC

# Optional: Override port if needed (default is 5000)
# PORT=5000
